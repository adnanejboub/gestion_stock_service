server:
  port: 8084

spring:
  application:
    name: commande-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_commandes?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    rest:
      base-path: /api/commandes/data

  graphql:
    graphiql:
      enabled: true
      path: /graphiql

# Configuration gRPC
grpc:
  server:
    port: 9090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
    metadata-map:
      grpc-port: 9090